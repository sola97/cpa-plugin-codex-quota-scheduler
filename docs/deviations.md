# Refactor deviations

| ID | Spec clause | Conflict | Conservative decision | Tests | Status |
| --- | --- | --- | --- | --- | --- |
| DEV-001 | Plan Task 1 RED | The Resource/Management boundary already satisfies INV-01, so forcing a production-code failure would weaken known-good behavior. | Characterization test + mutation validation replaces forced RED. S1 onward remains strict RED -> GREEN. | `TestSuiteBoundary/mutation_scanner_detects_a_test-owned_leak` | Accepted |
| DEV-002 | Plan Task 2 Step 5 | The current transitive pick path still reaches legacy refresh code, so immediate zero-violation enforcement would either misrepresent the path or pull the S5 rewrite into S1. | S1 uses a standard-library Go AST analyzer rooted at real `handleSchedulerPick`, discovers the same-package transitive call closure and import aliases, and checks a counted file/type/symbol ratchet baseline. S5 requires both baseline and actual closure violations to be empty. | `go test ./internal/refactorgate`; `./scripts/check_refactor_gates.ps1 -Stage S1 -VerifyNegativeFixture`; future `-Stage S5` | Accepted |
| DEV-003 | Design section 1.1 | An empty, missing, or null roster has no entry whose Priority presence can prove the authoritative Priority contract. | Treat `{}`, missing/null/empty `files`, malformed payloads, zero normalized entries, or any entry missing explicit Priority as Capability-B regardless of provider. | `TestSuiteCapabilityFallsBackForEmptyRoster`; `TestSuiteCapabilityRawRosterPayloadsCannotProveCapability`; `TestSuiteCapabilityFallsBackWhenNonCodexPriorityIsMissing` | Accepted |
| DEV-004 | Design section 11 external constants | Host callback readiness is undocumented and a hanging callback could otherwise leave startup detection unresolved forever. | Bound the startup probe at the fixed external constant `hostRosterDetectionTimeout = 5s`; timeout deterministically publishes Capability-B. Recovery remains owned by S7. | `TestSuiteCapabilityStartupTimeoutPublishesFallbackWithoutRealSleep`; future `-Stage S7` | Accepted |
| DEV-005 | S2 primitives to production | Production wiring had no stage owner. | Insert S2.5 to own runtime construction and dependency injection without expanding S3 behavior. | `TestSuiteRuntimeWiring` | Accepted |
| DEV-006 | Section 8 state file | Machine runtime state and user-maintained data have different ownership and recovery semantics. | Implement two semantic files, `.runtime-state.json` and `.user-data.json`; apply Section 8 rules to each as relevant. v0.2 migration was already required, so the rename has near-zero marginal cost; retain the old file as `.migrated`. | `TestRuntimeSemanticPathsAndLegacyMigration`; `TestUserDataMigrationCrashPointsConverge` | Accepted |
| DEV-007 | Fingerprint baseline | First observation had no explicit classification rule. | Authoritative roster observation plus coordinator-owned read-only GetAuth creates F0 and initializes epochs/generation without classifying an external login, incrementing epochs, unlocking AuthBlocked, SaveAuth, or OpenAI calls. | `TestBindingGenesisAndInjectedCredentialWAL`; `TestBindingGenesisFailsClosedForCapabilityBAndAuthBlocked` | Accepted |
| DEV-008 | §3.3 typed coordinator stage ownership | The implementation plan assigned no stage owner for typed `quota_read`/Probe coordinator evolution after the S3 legacy adapter. | S6 extends Coordinator additively with actual-start `ReadStartSeq`, barrier-aware read joining, attempt-aware/nonjoining Probe sends, and injectable propagation waits while preserving the S3 legacy envelope contract and tests unchanged. The `legacy_refresh_txn` wire label remains accepted only for `OperationLegacyRefresh` until that adapter is deleted; typed Probe can never use it. | `TestTypedQuotaReadAllocatesAtActualStartAndBarrierJoin`; `TestTypedProbeSendNeverJoinsSameAttempt`; `TestMockGroupETypedIntentInterleavings`; `TestMockGroupALegacyEnvelopeWithTypedIntent` | Accepted |

| DEV-009 | DEV-003; decision spec sections 1.1 and 4.1 | CPA 7.2.x serializes `HostAuthFileEntry.Priority` with `omitempty`, so its default priority `0` is indistinguishable from an omitted value in `host.auth.list`; non-Codex entries are outside this Codex scheduler's roster domain. | Ignore non-Codex entries at the ABI roster boundary and normalize missing Codex priority to explicit `0`. Empty/malformed/no-Codex rosters and callback failures remain Capability-B. This supersedes DEV-003 only for provider filtering and missing Codex priority interpretation. | `TestSuiteCapabilityABIMissingCodexPriorityDefaultsToZero`; `TestSuiteCapabilityABIIgnoresNonCodexEntries`; `TestSuiteCapabilityABIOnlyNonCodexRemainsFallback` | Approved 2026-07-14 |
| DEV-010 | Existing scheduler validity rules and Probe lifecycle | OpenAI may omit the general FiveHour window while still returning a valid Weekly or Monthly LongWindow. Treating FiveHour as mandatory makes otherwise authoritative quota data unusable. | FiveHour is optional when a valid LongWindow exists. Do not synthesize it. If present, retain reset/exhaustion validation. If LongWindow is missing or invalid, remain Unknown/unavailable. Successful quota observation removes obsolete FiveHour Probe state unless a nonterminal durable attempt still references it; recovery or the next refresh completes cleanup. | `TestParseCodexUsagePayloadAllowsSecondaryOnlyLongWindow`; `TestPickAllowsWeeklyWithoutFiveHour`; `TestPickRejectsAccountWithoutLongWindow`; `TestProbeRefreshRemovesAbsentFiveHourWindow`; `TestProbeRefreshPreservesAbsentFiveHourDuringNonterminalAttempt` | Approved 2026-07-14 |
| DEV-011 | S7 authoritative roster publication and runtime admission | `PublishAuthoritativeRoster` publishes the immutable scheduler snapshot but not `PluginState` CPA admission. Management/manual refresh therefore observes no admission and returns without scanning auths despite a healthy authoritative roster. | After roster filtering, binding reconciliation, genesis, persistence, removal fencing, and Probe recovery all succeed, atomically replace runtime admission with `{Observed:true, Priority:<highest>, AuthIDs:<highest tier>}` before exposing the roster as authoritative. Admission, scheduler snapshot, runtime roster, and credential-adapter roster form one linearizable publication boundary. Every deletion, demotion, or higher-tier replacement creates a new admission version so stale refresh results are fenced. Production candidate refresh cannot mutate admission. Failed publication remains fail-closed and does not authorize runtime calls. | `TestProductionRosterPublicationEnablesManualRefresh`; `TestProductionRosterReplacementReplacesAdmissionAndFencesStaleRefresh`; `TestProductionRosterPublicationDoesNotExposeNewAdmissionWithOldRoster`; `TestProductionCandidateRefreshCannotMutateAuthoritativeAdmission`; `TestProductionCandidateRefreshCannotOverwriteAdmissionDuringPublication`; `TestRosterPublicationStaysFailClosedUntilAllGenesisSucceeds` | Approved 2026-07-14 |
| DEV-012 | Selection availability classes and Management queue/status projection | Management sorts plugin priority before queue status, so exhausted high-priority accounts appear ahead of selectable low-priority accounts. Active temporary feedback also masks a more specific authoritative LongWindow exhaustion reason. | Reuse production `AccountView`/`ClassifyAccount` and `accountViewLess` for `Preferred` and `Opportunistic`. Put all `Excluded` accounts last, ignore plugin priority there, and sort them by known recovery time with unknown recovery last. Validate and classify LongWindow exhaustion before applying temporary feedback, without clearing the durable temporary marker. | `TestManagementQueueMatchesProductionAvailabilityClasses`; `TestExcludedManagementQueueIgnoresPluginPriorityAndSortsByRecovery`; `TestLongWindowExhaustionPrecedesTemporaryFeedbackWithoutFiveHour` | Approved 2026-07-15 |
| DEV-013 | Frozen all-Excluded fallback contract | A manually enabled account-level quota reserve is local to this plugin. Delegating an all-reserve request to CPA `fill-first` would let the host select the reserved account because it cannot see the local reserve rule. | When no active candidate is selectable and at least one active candidate is currently blocked by account-level reserve, `handleSchedulerPick` returns a scheduler error instead of delegating built-in fallback. The production snapshot, Management projection, and fallback check share the same inclusive `now <= unlock_at` boundary. | `TestSchedulerPickPublishedRejectsReservedCandidateAtUnlockBoundary`; `TestHandleSchedulerPickRejectsReservedCandidate`; `TestSchedulerPickPublishedKeepsFallbackWhenReserveIsNotBlocking` | Approved 2026-07-25 |

## Permanent execution-review rule

Reviewers judge the contract of the current frozen-plan stage. A finding owned
by a later stage becomes a machine-checkable acceptance item for that stage and
does not block the current stage unless the current-stage output itself
violates an invariant. The gate script encodes the currently known S5 and S7
acceptance items; S1 does not execute those future-stage branches.

### Future-stage machine checklist

- S5: `scripts/refactor_gates/s1-pick-path-baseline.json` must be empty; the
  INV-43 pick-path scan must report zero violations; and
  `TestSchedulerPickABIPathSnapshotOnly` must exercise the real ABI
  `scheduler.pick` path under snapshot-only constraints.
- S7: `TestStartupCapabilityBRecoversThroughRosterSynchronization` must prove
  that an initial startup Capability-B later becomes Capability-A through the
  roster-controller synchronization policy.
- S6: `ProbeAttemptSeam` must evolve as a strict superset or through an explicit
  runtime schema migration; `TestProbeAttemptSeamRuntimeRoundTrip` may not be removed.
